# require 'rails_helper'
#
# RSpec.describe PullRequest do
#   it "exists and has attributes" do
#     data = { name: 'Fake Repo' }
#     repo = PullRequest.new(data)
#     expect(repo).to be_an_instance_of(PullRequest)
#     expect(repo.count).to eq(1)
#   end
# end
