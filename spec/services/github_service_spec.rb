require 'rails_helper'

RSpec.describe GithubService, type: :model do
  it "returns the users data" do
    # mock_response = '{}'

    # allow(GithubService).to receive().and_return(mock_response)
    # allow_any_instance_of(Faraday::Response).to receive(:body).and_return(mock_response)
    # json = GithubService.user_info('user_name')

    # expect(json).to be_a(Hash)
    # expect(json).to have_key(:repo_name)
    # expect(json).to have_key(:user_name_1)
    # expect(json).to have_key(:user_name_2)
    # expect(json).to have_key(:user_name_3)
    # expect(json).to have_key(:user_name_4)
    # expect(json).to have_key(:commits)
    # expect(json).to have_key(:pull_requests)
  end
end
