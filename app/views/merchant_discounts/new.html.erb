<%= form_with url: "/merchants/#{@merchant.id}/discounts", method: :post, local: true do |f| %>
  <%= f.label :threshhold_quantity %>
  <%= f.integer :threshhold_quantity %>

  <%= f.label :discount_percentage %>
  <%= f.integer :discount_percentage %>

  <%= f.submit "Submit" %>
<% end %>
