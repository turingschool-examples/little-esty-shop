<h1>Invoice <%= @invoice.id %></h1>

Invoice status is: <%= @invoice.status %>

<%= form_with url: "/admin/invoices/#{@invoice.id}", method: :get do |f| %>
  Change invoice status: <%= f.select(:status, options_for_select([['in progress', 'in progress'], ['completed', 'completed'], ['cancelled', 'cancelled']])) %>
  <%= button_to 'Update Invoice Status', "/admin/invoices/#{@invoice.id}", method: :patch %>
<% end %>


<h3>Items:</h3>
<% @invoice.invoice_items.each do |invoice_item| %>
  <div id="item-id-<%= invoice_item.item.id %>">
    <p>
      Item name: <%= invoice_item.item.name %><br>
      Quantity of <%= invoice_item.item.name %>(s) ordered: <%= invoice_item.quantity %><br>
      Total price of the <%= invoice_item.item.name %>(s): <%= invoice_item.unit_price %><br>
      Status: <%= invoice_item.status %><br>
      <br>
    </p>
  </div>
<% end %>

<h3>Total revenue expected: <%= InvoiceItem.total_revenue(@invoice) %>