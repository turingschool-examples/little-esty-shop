<h1>All Invoices</h1>

<h3><% @invoices.each do |invoice| %>
  <div id="invoice_link-<% invoice.id %>" >
    <ol>
      <li><%= link_to invoice.id, "/admin/invoices/#{invoice.id}" %></li>
    </ol>
  <% end %>
  </div>
</h3>
