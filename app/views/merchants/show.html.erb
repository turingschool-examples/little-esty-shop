<h1 class="merchant">Merchant Name: <%= @merchant.name %></h1>

<p><%= link_to "Items Index", "/merchants/#{@merchant.id}/items"%></p>
<p><%= link_to "Invoices Index", "/merchants/#{@merchant.id}/invoices"%></p>

<h3><div class="items-ready-to-ship">Items Ready To Ship</h3>
  <% @merchant.ready_items_dates.each do |item| %>
    <p><%= item.name %></p>
    <p>Invoice Created: <%= item.created_at.strftime("%A %B %m %Y") %></p>
    <% end %>
</div>

<%
=begin%>
 <h3><div class="favorite-customers">Favorite Customers</h3>
  <% @merchant.favorite_customers.each do |customer| %>
    <%= customer.name %>
    <% end %>
</div> 
<%
=end%>
