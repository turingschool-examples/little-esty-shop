<%= render partial: 'partials/merchants_invoice_nav_bar' %>

<div class="body_container_merchant_item">
  <div class="sub_container_merchant_item">
    <div class="sub_header_merchant_item">My Invoices</div>
      <div id="merchant_invoices">
        <% @invoices.each do |invoice| %>
          <p><%= link_to "Invoice ##{invoice.id}", merchant_invoice_path(@merchant, invoice) %></p>
        <% end %>
      </div>
  </div>
</div>