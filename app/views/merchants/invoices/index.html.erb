<div class="container text-center">
<h1>Merchant Invoices</h1>
<% @merch_invoice.each do |invoice| %>
  <p><%=link_to "Invoice Number: #{invoice.id}", "/merchants/#{@merchant.id}/invoices/#{invoice.id}" %>
    <%="Order Status: #{invoice.status}" %></p>
<% end  %>
</div>
