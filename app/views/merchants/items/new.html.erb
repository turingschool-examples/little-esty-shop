<%= form_with url: merchant_items_path(@merchant), method: :post, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "Enter Name" %><br>

  <%= f.label :description %>
  <%= f.text_area :description, placeholder: "Enter Description" %><br>

  <%= f.label :unit_price %>
  <%= f.number_field :unit_price, step: 0.01, min: 0, placeholder: "Enter Selling Price" %>

  <%= f.submit 'Create Item' %>
<% end %>
