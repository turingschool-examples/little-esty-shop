<h1>My Items</h1>
<div id="merchant_items">
  <ol>
  <% @items.each do |item| %>
    <div id="merchant_item_<%= item.id %>"> 
      <li><%= link_to item.name, "/merchants/#{@merchant.id}/items/#{item.id}" %></li>
    </div>
  <% end %>
  </ol>
</div>
