<%= render partial: 'form', locals: {
                                    path: merchant_bulk_discount_path(@merchant, @discount),
                                    method: :patch,
                                    value: @discount.threshold ,
                                    value2: @discount.discount_percentage ,
                                    button_text: "Submit Discount" } %>
