<h4><center><%= @item.name %> Edit Page</center><h4>

<%= render partial: 'form', locals: {
                                      path: "/merchants/#{@item.merchant.id}/items/#{@item.id}",
                                      method: :patch,
                                      button_text: "Submit #{@item.name} Update",
                                      name: @item.name,
                                      description: @item.description,
                                      unit_price: @item.unit_price } %>
                                  