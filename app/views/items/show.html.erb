<h1><%= @item.name %></h1>
<section id="item-info">
  <p>
    Description: <%= @item.description %>
  </p>
  <p>
    Current Price: $<%= @item.unit_price %>
  </p>
</section>
<section id="update-link">
  <%= link_to "Update Item", edit_merchant_item_path(@merchant, @item), method: :get %>  
</section>
