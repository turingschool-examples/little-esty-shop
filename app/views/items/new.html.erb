<%= form_with model: @item, local: true do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<%= f.label :unit_price %>
	<%= f.number_field :unit_price %>
	<%= f.submit %>
<% end %>
