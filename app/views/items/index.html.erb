<h1><%= @merchant.name %> Items</h1>


Enabled Items:
<ol>
    <% @enabled_items.each do |item| %>
       <li><%= item.name %> <%= button_to "Disable #{item.name}", merchant_item_path(@merchant, item), method: :patch, params: {id: @merchant.id} %></li>
    <% end %>
</ol>

Disabled Items:
<ol>
    <% @disabled_items.each do |item| %>
       <li><%= item.name %> <%= button_to "Enable #{item.name}", merchant_item_path(@merchant, item), method: :patch, params: {id: @merchant.id} %></li>
    <% end %>
</ol>



<% @merchant.top_5_items.each do |item| %>
       <%= item.name %>, <%= item.revenue %>
        <% end %>