<h1>My Items</h1>

<br>

<% @items.each do |item| %>
  <p><%= link_to item.name, merchant_item_path(@merchant.id, item.id) %></p>
<% end %>

<section>
<h3>Top 5 Items:</h3>
  <% @merchant.top_five_items.each do |item| %>
    <p><%= item.name %></p>
  <% end %>
</section>
