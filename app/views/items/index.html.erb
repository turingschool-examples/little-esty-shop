<h1><%= @merchant.name %> Items</h1>


Enabled Items:
<ol>
    <% @merchant.items.each do |item| %>
       <li><%= item.name %> <%= button_to "Disable #{item.name}", merchant_item_path(@merchant, item), method: :patch, params: {id: @merchant.id} %></li>
    <% end %>
</ol>

Disabled Items: