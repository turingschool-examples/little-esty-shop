<h2><%= @merchant.name %></h3>
<h3>My Items</h3>

<%= link_to 'New Item', new_merchant_item_path(@merchant) %>
<%= render partial: "items", locals: { path: merchant_path(@merchant) } %>
<%= render partial: "topfive", locals: { path: merchant_path(@merchant) } %>
