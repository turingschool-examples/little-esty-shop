<h1><%= @merchant.name %>'s Items</h1>

<body>
  <section id="merchant-items-list">
    <% @merchant.items.each do |item| %>
      <div id="item-<%= item.id %>">
        <p><%= item.name %></p>
    <% end %>
      </div>
  </section>

</body>