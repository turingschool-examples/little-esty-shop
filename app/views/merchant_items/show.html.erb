<h1><%= @item.name %></h1>
<section id="item-info">
  <p>
    Description: <%= @item.description %>
  </p>
  <p>
    Current Price: $<%= @item.unit_price %>
  </p>
</section>
<section id="update-link">
  <%= link_to "Update Item", merchant_item_edit_path(@merchant, @item), method: :get %>  
</section>
