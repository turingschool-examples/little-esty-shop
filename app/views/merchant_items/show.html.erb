<p><%= flash[:update_status] if flash[:update_status] %></p>

<h2><%= @item.name %></h2>
<p><%= @item.description %></p>
<p><%= @item.unit_price %></p>

<p><%= link_to "Update #{@item.name}", edit_merchant_item_path(@merchant, @item) %></p>
