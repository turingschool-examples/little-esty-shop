<h1><%= @item.name %></h1>

<h3>Description: <%= @item.description %></h3>
<h3>Unit Price: <%= @item.unit_price %></h3>

<%= link_to "Update Item", "/merchants/#{@merchant.id}/items/#{@item.id}/edit" %>

<div class="github-info">
  <p> Repo name: <%= @repo_name.name %> </p>
</div>
