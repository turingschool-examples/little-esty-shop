<header id="admin_header">
  <h2 >Admin Dashboard</h2>
</header>
<nav id="admin_nav">
  <%=  link_to "Merchants", '/admin/merchants' %>
  <%=  link_to "Invoices", '/admin/invoices' %>
</nav>

<section id="top_customers">
  <h4>Top Customers</h4>
  <ol>
    <% @top_customers.each do |customer|  %>
      <li><%= customer.full_name %>- <%= customer.successful_order %> purchases</li>
    <% end %>
  </ol>
</section>

<section id="incomplete_invoices">
  <h3>Incomplete Invoices</h3>
  <% @unshipped.each do |invoice_item| %>
    <ul id="<%= invoice_item.invoice_id %>">
      <li> Invoice #<%= link_to "#{invoice_item.invoice_id}", "/admin/invoices/#{invoice_item.invoice_id}" %></li>
    </ul>
  <% end %>
</section>