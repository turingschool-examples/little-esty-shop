<%= render partial: 'form', locals: { method: :patch,
                                      url: admin_merchant_path(@merchant),
                                      value: "#{@merchant.name}",
                                      local: true } %>
