<h1>Editing Merchant <%= @merchant.name %></h1>

<%= form_with(model: @merchant, url: admin_merchant_path(@merchant), method: :patch,local: true) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.submit %>

<% end %>
