<h1><%= @merchant.name %> </h1>

<%= form_with model: [:admin, @merchant], method: :patch, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name, value: @merchant.name %>
  <%= form.submit "Submit" %>
<% end %>