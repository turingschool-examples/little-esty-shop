<%= form_with url: "/admin/merchants/#{@merchant.id}", method: :patch, local: true do |f| %>
<%= f.label :name %>
<%= f.text_field :name, placeholder: "#{@merchant.name}" %>
<%= f.label :id %>
<%= f.number_field :id, placeholder: "#{@merchant.id}"  %>
<%= f.submit "Submit" %>
<%end %>
