<h1> All Merchants </h1>
</br>

<% @merchants.each do |merchant| %>
  <section id="all-admin-merchants-<%= merchant.id %>">
  <h3> <%= link_to "#{merchant.name}", admin_merchant_path(merchant.id) %> <h3>
  </section>
<% end %>