<h1> Merchants </h1>
<% @merchants.each do |merchant| %>
<div id="merchant_name-<%= merchant.id %>"> 
  <%= link_to "#{merchant.name}", "/admin/merchants/#{merchant.id}"  %>
  <div id= "change_status">
  <p>Status: <%= merchant.status %></p>
  <%= button_to "Disable/Enable", admin_merchant_path(merchant), method: :patch, params: {swap: "true", status: merchant.status} %>
  </div>
</div>
<% end %>
