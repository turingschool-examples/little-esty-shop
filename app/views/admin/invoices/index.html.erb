<h1>Admin Invoices Index Page</h1>
<% @invoices.each_with_index do |invoice, index| %>
  <div id="admin_invoice-<%= index %>">
  <%= link_to "Invoice id: #{invoice.id}", admin_invoice_path(invoice.id) %><br><br>
<% end %>
