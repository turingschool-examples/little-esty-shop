<%= render partial: "header" %>

<ul>
<h2>All Invoices</h2>
  <% @invoices.each do |invoice| %>
  <p id="invoice-<%= invoice.id %>">
    Invoice #<%= link_to "#{invoice.id}", admin_invoice_path(invoice) %></p>
  <% end %>
</ul>
