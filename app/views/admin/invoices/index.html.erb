<h1>admin invoices index page </h1>

<% @invoices.each do |invoice| %>
   <div id="invoice-<%= invoice.id %>">
      <%= link_to "#{invoice.id}", admin_invoices_path(invoice.id) %>
   </div>
<% end %>