<h1>Welcome to the Admin Dashboard</h1>
<%= link_to "Merchants", '/admin/merchants' %>
<%= link_to "Invoices", '/admin/invoices' %>

<ol>
  <% @customers.top_customers.each do |customer| %>
    <section id="customer-<%= customer.id %>">
      <li><%= "#{customer.first_name} #{customer.last_name} - #{customer.num_transactions} Purchases" %></li>
    </section>
  <% end %>
</ol>
