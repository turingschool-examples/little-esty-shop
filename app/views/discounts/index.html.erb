<h1>Discounts for: <%= @merchant.name %></h1>
<% @discounts.each do |discount| %>
    <h3>Discount Link: <%= link_to "#{discount.id}", "/merchants/#{@merchant.id}/discounts/#{discount.id}" %></h3>
    <p>Discount Quantity Threshhold: <%= discount.quantity_threshhold %></p>
    <p>Discount Percentage: <%= discount.percentage %></p>

<% end %>