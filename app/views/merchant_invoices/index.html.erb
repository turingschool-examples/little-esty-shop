<h3> <%= @merchant.name %> </h3>

<div>
    <h3> Invoices </h3>
    <% @merchant.invoices.distinct.each do |invoice| %>
        <div id="invoice-<%= invoice.id %>" >
            <p> <%= invoice.id %> </p>
        </div>
    <% end %>
</div>