<h2>Invoices:</h2>

<% @invoices.each do |invoice| %>
  <div id="invoice_ids">
    <p>Invoice: <%= link_to "#{invoice.id}", "/merchants/#{@merchant.id}/invoices/#{invoice.id}" %></p>
  </div>
<% end %>
