<h1><%= "#{@merchant.name}'s Invoices" %></h1>

<h2>Invoices</h2>

<% Invoice.merchants_invoices(@merchant.id).each do |inv| %>
<section id="invoice-<%= inv.id %>"> 
  <p><%= "Invoice # " %>
  <%= link_to "#{inv.id}", merchant_invoice_path(@merchant, inv) %></p>

<% end %>
</section>