<h1>My Invoices</h1>
<section id="merchant-invoices">
  <% @merchant_invoices.each do |invoice| %>
    <p>Invoice #<%= link_to "#{invoice.id}", merchant_invoice_path(@merchant, invoice), method: :get %></p>
  <% end %>
</section>
