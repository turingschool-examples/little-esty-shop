<!DOCTYPE html>
<html>
  <head>
    <title>LittleEtsyShop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <footer>
      <% @github.repo_name %>
      <% @github.users.each do user %>
        <%= user.name %> commits: <%= user.commits%>
      <% end %>
      Merged PR's: <% @github.pr_count%>
    </footer>
  </body>
</html>
