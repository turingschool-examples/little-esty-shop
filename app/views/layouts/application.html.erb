<!DOCTYPE html>
<html>
  <head>
    <title>LittleEtsyShop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1>Little Esty Shop</h1>
    <%= yield %>
    <footer>
      GitHub Repository Name: <%= @search.repo_information.name %><br>
      GitHub Usernames of Team Members: 
      <ul>
      <% @search.contributors.each do |contributor| %>
        <li><%= contributor.username %>, commit count: <%= @search.commit_count(username) %></li>
      <% end %>
      </ul>
    </footer>
  </body>
</html>
