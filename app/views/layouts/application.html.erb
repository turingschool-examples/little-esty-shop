<!DOCTYPE html>
<html>
  <head>
    <title>LittleEtsyShop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application' %>
  </head>

  <body>
  <% flash.each do |type, message| %>
      <p><%= message %></p>
    <% end %>
    <h1 id="welcome_header">Little Esty Shop</h1>
    <%= yield %>
    <footer>
    <div>
    <%= commit_count %>
    <p> Repo name: <%= repo_name %>&emsp; Contributors: <%= user_name %>&emsp; Total PR's: <%= pull_request %> </p>
    </div>
    </footer>
  </body>
</html>
