<!DOCTYPE html>
<html>
  <head>
    <title>LittleEtsyShop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= GithubFacade.repo_name[:name] %>

    <% flash.each do |type, message| %>
      <p><%= message %></p>
    <% end %>
    <%= yield %>
  </body>

  <footer>
    GitHub User Information:<br>
    <% GithubFacade.get_users.each do |user| %>
        <%= user.username %> : <%= user.total_commits %><br>
    <% end %>
  </footer>
</html>
