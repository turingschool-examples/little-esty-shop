<% if @repo.present? %>
  <% if @repo.is_a?(Repository) %>
    <p><%= @repo.name %></p>
    <% else %>
    <p><%= @repo[:message] %></p>
    <% end %> 
<% end %> # this accounts for the error message 